// ─────────────────────────────────────────────────────────────────────
// AUREA — brand identity (by TMH GROUP). Single source of truth so the
// name lives in ONE place: swap these strings to rebrand everything
// (TopNav logo, Home hero, document title…).
// ─────────────────────────────────────────────────────────────────────

export const BRAND = {
  /** Primary wordmark shown big in the TopNav + hero. */
  name: 'AUREA',
  /** Small caps line under the wordmark. */
  tagline: 'TMH GROUP · AI STUDIO',
  /** Parent company. */
  company: 'TMH GROUP',
  /** One-line positioning used on the Home hero subtitle. */
  pitch: 'Avatar AI · Giọng Việt · Kịch bản · Lipsync — tất cả trong một workflow.',
} as const
