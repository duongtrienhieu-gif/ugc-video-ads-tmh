// ── WhatsApp Reference Exemplars (P5) ───────────────────────────────────────
//
// Pointers to reference screenshots the template / QC layer can consult.
// P5 ships an empty pool — exemplar URLs are added when a curated
// reference library is uploaded to public/. The pool is wired so the
// QC step does not need a code change to consume new exemplars.

import type { ReferenceExemplar } from '../../../types/uiNative'

export const WHATSAPP_EXEMPLARS: ReferenceExemplar[] = []
